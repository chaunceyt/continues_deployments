## README.txt for continues_deployments

Install Java and the latest version of Jenkins.
Configure Apache ProxyPass virtual host
Configure Global security
Install composer
  curl -sS https://getcomposer.org/installer | php -d detect_unicode=Off
  
